import PptxGenJS from 'pptxgenjs'
import type { TaskStore } from './store.js'
import type { Task } from './types.js'

const escapeHtml = (value: string) => value.replaceAll('&', '&amp;').replaceAll('<', '&lt;').replaceAll('>', '&gt;')

const shell = (title: string, body: string, script = '') => `<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>${escapeHtml(title)}</title><style>:root{font-family:Inter,system-ui;background:#090b0a;color:#f3f6f4}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 75% 10%,#143523,transparent 35%),#090b0a}main{width:min(1060px,92vw);margin:auto;padding:56px 0}.eyebrow{font:11px ui-monospace;color:#42db82;letter-spacing:.14em;text-transform:uppercase}h1{font-size:clamp(42px,7vw,82px);line-height:.94;letter-spacing:-.06em;margin:20px 0}p{color:#9aa59e;line-height:1.65}.card{border:1px solid #29332d;background:#101411;border-radius:14px;padding:22px}.grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(210px,1fr));gap:10px;margin-top:30px}button{border:1px solid #375241;background:#15251b;color:#a5e3bd;border-radius:8px;padding:9px 12px;cursor:pointer}</style></head><body><main>${body}</main>${script ? `<script>${script}</script>` : ''}</body></html>`

const slideOutlineFor = (task: Task): Array<[string, string]> => {
  const brief = task.prompt.replace(/\s+/g, ' ').trim()
  const focus = brief.length > 280 ? `${brief.slice(0, 277)}…` : brief
  return [
    [task.title, `Executive briefing prepared from the requested outcome: ${focus}`],
    ['The decision in view', `What the audience needs to understand, decide, or align on: ${focus}`],
    ['Context and opportunity', 'Frame the operating environment, the people affected, and why acting now matters. Separate established facts from assumptions requiring validation.'],
    ['Proposed approach', 'Describe the recommended path in concrete terms: scope, user experience, operating boundary, and what is intentionally out of scope.'],
    ['How the work will happen', 'Lay out the delivery sequence, accountable owners, dependencies, and the evidence needed at each meaningful checkpoint.'],
    ['Risk, policy, and controls', 'Identify material risks, policy constraints, and actions that require a separate accountable decision before execution or release.'],
    ['Measures of progress', 'Define observable outcomes, leading indicators, and review points. Avoid treating unverified activity as proof of value or safety.'],
    ['Recommended next step', 'Confirm the immediate owner, decision required, and the smallest governed action that moves the work forward.'],
  ]
}

const writeSlides = async (task: Task, store: TaskStore) => {
  const slides = slideOutlineFor(task)
  const moduleValue: unknown = PptxGenJS
  const PptxConstructor = typeof moduleValue === 'function' ? moduleValue : (moduleValue as { default: unknown }).default
  // PptxGenJS has different default-import shapes under native Node and TSX/Vitest transforms; both expose this constructor.
  // @ts-expect-error The runtime shape is verified in both unit and live API integration tests.
  const deck = new PptxConstructor()
  deck.layout = 'LAYOUT_WIDE'
  deck.author = 'ONEVibe'
  deck.subject = task.prompt
  for (const [title, summary] of slides) {
    const slide = deck.addSlide()
    slide.background = { color: '090B0A' }
    slide.addShape(deck.ShapeType.rect, { x: 0.45, y: 0.45, w: 0.08, h: 0.35, fill: { color: '38DC7D' }, line: { color: '38DC7D' } })
    slide.addText('ONEVIBE / GOVERNED AGENT WORKSPACE', { x: 0.7, y: 0.43, w: 6, h: 0.3, fontFace: 'Aptos Mono', fontSize: 9, color: '38DC7D', charSpacing: 1.5 })
    slide.addText(title, { x: 0.7, y: 1.55, w: 11.8, h: 1.5, fontFace: 'Aptos Display', fontSize: 34, bold: true, color: 'F3F6F4', breakLine: false, margin: 0 })
    slide.addText(summary, { x: 0.72, y: 3.25, w: 9.7, h: 1.1, fontFace: 'Aptos', fontSize: 17, color: '9AA59E', margin: 0, breakLine: false })
    slide.addText(`${slides.indexOf(slides.find((item) => item[0] === title)!) + 1}`.padStart(2, '0'), { x: 11.9, y: 6.75, w: 0.7, h: 0.3, fontFace: 'Aptos Mono', fontSize: 9, color: '647068', align: 'right' })
  }
  const bytes = await deck.write({ outputType: 'uint8array', compression: true })
  if (!(bytes instanceof Uint8Array)) throw new Error('PPTX generator returned an unexpected output type')
  await store.writeWorkspaceBytes(task.id, 'deck.pptx', bytes)
  await store.writeWorkspaceFile(task.id, 'outline.json', `${JSON.stringify(slides.map(([title, summary], index) => ({ number: index + 1, title, summary })), null, 2)}\n`)
  await store.writeWorkspaceFile(task.id, 'speaker-notes.md', slides.map(([title, summary], index) => `## ${index + 1}. ${title}\n\n${summary}\n`).join('\n'))
  const cards = slides.map(([title, summary], index) => `<article class="card slide" data-slide="${index}"><div class="eyebrow">Slide ${index + 1} / ${slides.length}</div><h1>${escapeHtml(title)}</h1><p>${escapeHtml(summary)}</p></article>`).join('')
  await store.writeWorkspaceFile(task.id, 'index.html', shell('ONEVibe deck', `<div id="deck">${cards}</div><p><button id="previous">Previous</button> <button id="next">Next</button></p>`, `const slides=[...document.querySelectorAll('.slide')];let active=0;function show(){slides.forEach((s,i)=>s.hidden=i!==active)};previous.onclick=()=>{active=(active-1+slides.length)%slides.length;show()};next.onclick=()=>{active=(active+1)%slides.length;show()};show()`))
  return ['index.html', 'outline.json', 'speaker-notes.md', 'deck.pptx']
}

const writeScaffold = async (task: Task, store: TaskStore) => {
  const game = task.mode === 'game'
  const website = task.mode === 'website'
  const brief = `${task.title} ${task.prompt}`.toLowerCase()
  const storefront = task.mode === 'app' && /e-?commerce|storefront|shopping|cart/.test(brief)
  const dashboard = task.mode === 'app' && /dashboard|operations|priority queue|internal tool/.test(brief)
  const safeTitle = JSON.stringify(task.title)
  const appSource = game
    ? `import { useState } from 'react'
export default function App(){const [score,setScore]=useState(0);const [round,setRound]=useState(1);const [message,setMessage]=useState('Find the signal');const catchSignal=()=>{const next=score+round;setScore(next);setMessage(next>=12?'Signal secured — start a new round':'Signal captured');if(next>=12){setRound(1);setScore(0)}else setRound(round+1)};return <main className="game"><p className="eyebrow">ONEVibe playable prototype</p><h1>${task.title.replaceAll('`', '')}</h1><section><div><span>Round</span><strong>{round}</strong></div><div><span>Signal</span><strong>{score}/12</strong></div></section><button className="signal" onClick={catchSignal}>✦</button><p>{message}</p><small>This local interaction is portable source. Any publish action remains subject to an external approval.</small></main>}
`
    : website
      ? `const title=${safeTitle}
const safeguards=['Isolated workspaces','Clear policy checkpoints','Evidence that travels with the work']
const questions=[['Can teams bring their own agent?','Yes. This starter is designed around an operating boundary, not a single agent vendor.'],['Who approves consequential actions?','A separate approval system should decide. The generated site never treats the browser session as approval authority.'],['What happens to the work?','Portable source, files, and task evidence remain available for review and handoff.']]
export default function App(){return <main><nav><a className="brand" href="#top"><i>◈</i> ONEVibe</a><div className="links"><a href="#approach">Approach</a><a href="#safeguards">Safeguards</a><a href="#faq">FAQ</a></div><a className="nav-cta" href="#start">Explore the workspace <span>↗</span></a></nav><section id="top" className="hero"><p className="eyebrow">A GOVERNED WORKSPACE FOR CAPABLE AGENTS</p><h1>{title}</h1><p className="lede">Give people room to create with the agents they choose—while keeping meaningful controls visible, deliberate, and outside the browser.</p><div className="actions"><a className="primary" href="#start">See the operating model <span>↓</span></a><a className="secondary" href="#faq">Read the questions <span>→</span></a></div><div className="signal-card"><span className="pulse"/><p>Workspace boundary active</p><b>Policy and evidence connected</b></div></section><section id="approach" className="statement"><p className="eyebrow">BUILT FOR THE MOMENT AFTER THE PROMPT</p><h2>Useful work deserves a trustworthy place to happen.</h2><p>ONEVibe keeps the task, its workspace, and the evidence of what happened in one calm, reviewable surface.</p></section><section id="safeguards" className="safeguards">{safeguards.map((item,index)=><article key={item}><small>0{index+1}</small><h3>{item}</h3><p>{index===0?'One task receives a bounded place to work.':index===1?'Sensitive steps can pause for an independent decision.':'Source and context are ready for the next person to inspect.'}</p></article>)}</section><section id="start" className="callout"><p className="eyebrow">THE WORKSPACE</p><h2>Move quickly. Know where the boundary is.</h2><p>Start with a focused task. Keep the operational record close. Escalate only the actions that need a human decision.</p><a className="primary" href="#faq">Open the task brief <span>→</span></a></section><section id="faq" className="faq"><div><p className="eyebrow">COMMON QUESTIONS</p><h2>Designed to be understood.</h2></div><div>{questions.map(([question,answer])=><details key={question}><summary>{question}<span>+</span></summary><p>{answer}</p></details>)}</div></section><footer><span>ONEVIBE / PORTABLE WEBSITE STARTER</span><a href="#top">Back to top ↑</a></footer></main>}
`
      : storefront
        ? `import { useState } from 'react'
import { Button } from './components/ui/Button'
const products=[{id:'signal',name:'Signal desk',price:320,detail:'A focused workstation for deep work.'},{id:'atlas',name:'Atlas lamp',price:185,detail:'Warm light for long planning sessions.'},{id:'field',name:'Field notebook',price:28,detail:'A compact paper companion for the workday.'}]
export default function App(){const [cart,setCart]=useState<string[]>([]);const total=cart.reduce((sum,id)=>sum+(products.find(product=>product.id===id)?.price??0),0);return <main className="min-h-screen bg-stone-950 p-6 text-stone-100 sm:p-10"><header className="mx-auto flex max-w-5xl items-center justify-between border-b border-white/10 pb-5"><div><p className="text-xs font-mono tracking-[.18em] text-emerald-300">DEMO STOREFRONT</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">${task.title.replaceAll('`', '')}</h1></div><div className="rounded-full border border-emerald-300/30 bg-emerald-300/10 px-4 py-2 text-sm">Cart · {cart.length} · $ {total}</div></header><section className="mx-auto grid max-w-5xl gap-4 py-10 md:grid-cols-3">{products.map(product=><article key={product.id} className="grid gap-5 rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="aspect-[4/3] rounded-xl bg-gradient-to-br from-emerald-300/30 to-stone-900"/><div><h2 className="text-lg font-medium">{product.name}</h2><p className="mt-2 text-sm leading-6 text-stone-400">{product.detail}</p></div><div className="flex items-center justify-between"><strong>$ {product.price}</strong><Button onClick={()=>setCart(current=>[...current,product.id])}>Add to cart</Button></div></article>)}</section><footer className="mx-auto max-w-5xl border-t border-white/10 pt-5 text-sm text-stone-500">Fictional product data only. Payment, customer identity, and publishing are outside this generated prototype.</footer></main>}
`
        : dashboard
          ? `import { useState } from 'react'
import { Button } from './components/ui/Button'
const work=[{id:'OPS-124',title:'Review workspace request',owner:'Nadia',status:'needs review'},{id:'OPS-123',title:'Confirm delivery evidence',owner:'Luca',status:'in progress'},{id:'OPS-119',title:'Archive completed brief',owner:'Mina',status:'complete'}] as const
type Filter='all'|'needs review'|'in progress'|'complete'
const initialFilter:Filter='all'
export default function App(){const [filter,setFilter]=useState(initialFilter);const visible=work.filter(item=>filter==='all'||item.status===filter);return <main className="min-h-screen bg-stone-950 p-6 text-stone-100 sm:p-10"><header className="mx-auto flex max-w-5xl flex-wrap items-end justify-between gap-5"><div><p className="text-xs font-mono tracking-[.18em] text-emerald-300">GOVERNED OPERATIONS</p><h1 className="mt-2 text-3xl font-semibold tracking-tight">${task.title.replaceAll('`', '')}</h1><p className="mt-2 max-w-xl text-sm leading-6 text-stone-400">A focused queue with explicit status, fictional sample data, and no implied authority over external systems.</p></div><Button onClick={()=>setFilter('needs review')}>Focus review queue</Button></header><section className="mx-auto mt-9 max-w-5xl rounded-2xl border border-white/10 bg-white/[.04] p-4"><div className="mb-4 flex flex-wrap gap-2">{(['all','needs review','in progress','complete'] as const).map(status=><button key={status} onClick={()=>setFilter(status)} className={filter===status?'rounded-full border border-emerald-300 bg-emerald-300/15 px-3 py-1.5 text-xs text-emerald-100':'rounded-full border border-white/10 px-3 py-1.5 text-xs text-stone-400'}>{status}</button>)}</div><div className="grid gap-2">{visible.map(item=><article key={item.id} className="grid grid-cols-[auto_1fr_auto] items-center gap-4 rounded-xl border border-white/10 px-4 py-4"><code className="text-xs text-emerald-300">{item.id}</code><div><h2 className="text-sm font-medium">{item.title}</h2><p className="mt-1 text-xs text-stone-500">Owner · {item.owner}</p></div><span className="rounded-full bg-white/5 px-2 py-1 text-xs text-stone-300">{item.status}</span></article>)}</div></section></main>}
`
          : `import { useState } from 'react'
import { Button } from './components/ui/Button'
const roles=['Admin','Manager','Employee'] as const
const initialRole:(typeof roles)[number]='Admin'
export default function App(){const [role,setRole]=useState(initialRole);const copy=role==='Admin'?'Set company policy and approve workspace patterns.':role==='Manager'?'Set team guardrails and review exceptions.':'Start a bounded workspace and complete assigned work.';return <main className="min-h-screen bg-stone-950 p-8 text-stone-100"><section className="mx-auto grid max-w-3xl gap-7"><div><p className="text-xs font-mono tracking-[.18em] text-emerald-300">GOVERNED ROLE JOURNEY</p><h1 className="mt-3 text-4xl font-semibold tracking-tight">${task.title.replaceAll('`', '')}</h1><p className="mt-3 max-w-xl text-sm leading-6 text-stone-400">A small prototype of the hand-off between enterprise policy, team guardrails, and an employee workspace.</p></div><div className="flex flex-wrap gap-2">{roles.map(item=><button key={item} onClick={()=>setRole(item)} className={role===item?'rounded-full bg-emerald-300 px-4 py-2 text-sm font-semibold text-emerald-950':'rounded-full border border-white/15 px-4 py-2 text-sm text-stone-300'}>{item}</button>)}</div><article className="rounded-2xl border border-emerald-300/25 bg-emerald-300/10 p-6"><p className="text-xs font-mono uppercase tracking-[.14em] text-emerald-300">{role} perspective</p><h2 className="mt-3 text-2xl font-medium">{copy}</h2><p className="mt-3 text-sm leading-6 text-stone-300">This prototype keeps consequential approval outside the browser and records work in the governed task evidence chain.</p><Button className="mt-5" onClick={()=>setRole(role==='Employee'?'Admin':'Employee')}>See next role</Button></article></section></main>}
`
  const baseStyles = game
    ? ':root{font-family:system-ui;color:#eef3ef;background:#090b0a}*{box-sizing:border-box}body{margin:0}.game{min-height:100vh;padding:10vw;display:grid;place-content:center;gap:18px;text-align:center;background:radial-gradient(circle at 50% 25%,#163c26,transparent 35%),#090b0a}.eyebrow{color:#38dc7d;font:11px ui-monospace;letter-spacing:.12em;text-transform:uppercase}.game h1{max-width:720px;margin:0;font-size:clamp(38px,6vw,72px);letter-spacing:-.06em}.game section{display:flex;justify-content:center;gap:30px}.game section div{display:grid;gap:3px}.game span,.game small{color:#9ba79f;font-size:12px}.game strong{font-size:26px}.signal{width:110px;height:110px;margin:auto;border:1px solid #57df91;border-radius:50%;background:#143723;color:#b8ffcf;font-size:42px;cursor:pointer;box-shadow:0 0 55px #1a8b4c66;transition:transform .15s}.signal:hover{transform:scale(1.07)}'
    : website
      ? ':root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;color:#edf3ef;background:#0a0c0b}*{box-sizing:border-box}html{scroll-behavior:smooth}body{margin:0;background:radial-gradient(ellipse 75% 40% at 85% 0,#17372255,transparent 70%),#0a0c0b}a{color:inherit;text-decoration:none}a:focus-visible,button:focus-visible,summary:focus-visible{outline:2px solid #5be391;outline-offset:4px}main{overflow:hidden}nav,.hero,.statement,.safeguards,.callout,.faq,footer{width:min(1160px,calc(100% - 48px));margin:auto}nav{height:82px;display:flex;align-items:center;justify-content:space-between;border-bottom:1px solid #ffffff13}.brand{font-size:14px;font-weight:750;letter-spacing:-.04em}.brand i{display:inline-grid;place-items:center;width:23px;height:23px;margin-right:8px;border:1px solid #39d87d;border-radius:6px;color:#39d87d;font-style:normal}.links{display:flex;gap:25px;color:#9aa49d;font-size:13px}.nav-cta{padding:10px 13px;border:1px solid #ffffff1c;border-radius:8px;font-size:12px;font-weight:650}.nav-cta span,.primary span,.secondary span{margin-left:7px;color:#52df8c}.hero{position:relative;min-height:650px;padding:138px 0 100px}.eyebrow{margin:0;color:#5be391;font:10px ui-monospace,SFMono-Regular,monospace;letter-spacing:.15em}.hero h1{max-width:780px;margin:20px 0;font-size:clamp(56px,8vw,116px);line-height:.9;letter-spacing:-.075em}.lede{max-width:575px;color:#a6afa9;font-size:18px;line-height:1.65}.actions{display:flex;gap:12px;margin-top:36px}.primary,.secondary{display:inline-flex;align-items:center;padding:13px 17px;border-radius:8px;font-size:13px;font-weight:700}.primary{background:#40d978;color:#08100a}.secondary{border:1px solid #ffffff22;color:#dfe6e1}.signal-card{position:absolute;right:6%;bottom:105px;width:255px;padding:18px;border:1px solid #47d77d77;border-radius:12px;background:#0d1710cc;box-shadow:0 20px 60px #0008;backdrop-filter:blur(12px)}.signal-card p{display:inline;margin-left:8px;color:#b7c1bb;font:10px ui-monospace}.signal-card b{display:block;margin-top:12px;font-size:14px}.pulse{display:inline-block;width:8px;height:8px;border-radius:50%;background:#4ce18a;box-shadow:0 0 0 5px #4ce18a20}.statement{padding:118px 0 76px;border-top:1px solid #ffffff12}.statement h2,.callout h2,.faq h2{max-width:770px;margin:17px 0;font-size:clamp(34px,5vw,66px);line-height:.98;letter-spacing:-.055em}.statement>p:last-child,.callout>p:not(.eyebrow){max-width:570px;color:#9fa9a2;line-height:1.7}.safeguards{display:grid;grid-template-columns:repeat(3,1fr);gap:1px;background:#ffffff14;border:1px solid #ffffff14}.safeguards article{min-height:245px;padding:29px;background:#0e1110}.safeguards small{color:#56dd8e;font:11px ui-monospace}.safeguards h3{margin:54px 0 11px;font-size:21px;letter-spacing:-.04em}.safeguards p{color:#99a39c;font-size:14px;line-height:1.6}.callout{margin-top:96px;padding:74px;position:relative;border:1px solid #ffffff16;border-radius:16px;background:linear-gradient(120deg,#153721,#0d1310 55%)}.callout h2{max-width:620px}.faq{display:grid;grid-template-columns:1fr 1.15fr;gap:80px;padding:122px 0}.faq h2{font-size:46px}.faq details{border-top:1px solid #ffffff1b}.faq details:last-child{border-bottom:1px solid #ffffff1b}.faq summary{display:flex;justify-content:space-between;gap:20px;padding:21px 0;cursor:pointer;font-size:16px;font-weight:650;list-style:none}.faq summary::-webkit-details-marker{display:none}.faq summary span{color:#55df8d;font-size:22px;font-weight:400}.faq details[open] summary span{transform:rotate(45deg)}.faq details p{max-width:520px;margin:0;padding:0 0 21px;color:#9fa9a2;line-height:1.7;font-size:14px}footer{display:flex;justify-content:space-between;padding:23px 0 35px;border-top:1px solid #ffffff14;color:#77827a;font:10px ui-monospace;letter-spacing:.07em}footer a{color:#b6c0b9}@media(max-width:700px){nav{height:68px}.links{display:none}.nav-cta{display:none}.hero{min-height:auto;padding:92px 0 74px}.hero h1{font-size:clamp(52px,15vw,78px)}.signal-card{position:relative;right:auto;bottom:auto;margin-top:54px}.statement{padding-top:76px}.safeguards,.faq{grid-template-columns:1fr}.safeguards article{min-height:190px}.callout{margin-top:60px;padding:40px 27px}.faq{gap:35px;padding:82px 0}footer{gap:15px}.actions{flex-wrap:wrap}}@media(prefers-reduced-motion:reduce){html{scroll-behavior:auto}}'
      : ':root{font-family:system-ui;color:#eef3ef;background:#090b0a}body{margin:0}main{padding:10vw}button{padding:10px}'
  const styles = `@import "tailwindcss";\n${baseStyles}`
  const packageScripts = { dev: 'vite', build: 'tsc -b && vite build', preview: 'vite preview', ...(task.mode === 'app' ? { 'server:dev': 'tsx server/src/index.ts', 'server:check': 'tsc -p server/tsconfig.json --noEmit' } : {}) }
  const files: Record<string, string> = {
    'app/package.json': `${JSON.stringify({ private: true, type: 'module', scripts: packageScripts, dependencies: { react: '^19.2.7', 'react-dom': '^19.2.7' }, devDependencies: { '@types/node': '^24.13.3', '@types/react': '^19.2.17', '@types/react-dom': '^19.2.3', '@tailwindcss/vite': '^4.1.18', '@vitejs/plugin-react': '^6.0.3', tailwindcss: '^4.1.18', tsx: '^4.23.1', typescript: '~6.0.2', vite: '^8.1.1' } }, null, 2)}\n`,
    'app/vite.config.ts': "import { defineConfig } from 'vite'\nimport react from '@vitejs/plugin-react'\nimport tailwindcss from '@tailwindcss/vite'\nexport default defineConfig({ plugins: [react(), tailwindcss()] })\n",
    'app/tsconfig.json': `${JSON.stringify({ compilerOptions: { target: 'ES2022', jsx: 'react-jsx', strict: true, module: 'ESNext', moduleResolution: 'Bundler' }, include: ['src'] }, null, 2)}\n`,
    'app/src/main.tsx': "import React from 'react'\nimport { createRoot } from 'react-dom/client'\nimport './styles.css'\nimport App from './App'\ncreateRoot(document.getElementById('root')!).render(<App />)\n",
    'app/src/App.tsx': appSource,
    'app/src/styles.css': `${styles}\n`,
    'app/src/lib/cn.ts': "export const cn = (...values: Array<string | false | null | undefined>) => values.filter(Boolean).join(' ')\n",
    'app/src/components/ui/Button.tsx': "import type { ButtonHTMLAttributes } from 'react'\nimport { cn } from '../../lib/cn'\n\nexport const Button = ({ className, ...props }: ButtonHTMLAttributes<HTMLButtonElement>) => <button className={cn('inline-flex items-center justify-center rounded-md bg-emerald-400 px-3 py-2 text-sm font-semibold text-emerald-950 transition hover:bg-emerald-300 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-emerald-300', className)} {...props} />\n",
    'app/index.html': '<div id="root"></div><script type="module" src="/src/main.tsx"></script>\n',
    'app/.gitignore': 'node_modules\ndist\n.env*\n',
    'app/.prettierrc': '{"semi":false,"singleQuote":true}\n',
  }
  if (task.mode === 'app') Object.assign(files, {
    'app/src/shared/contracts.ts': "export type HealthResponse = { status: 'ok'; service: string }\nexport const healthResponse = (service: string): HealthResponse => ({ status: 'ok', service })\n",
    'app/server/tsconfig.json': `${JSON.stringify({ compilerOptions: { target: 'ES2022', module: 'NodeNext', moduleResolution: 'NodeNext', strict: true, types: ['node'], outDir: 'dist' }, include: ['src'] }, null, 2)}\n`,
    'app/server/src/index.ts': "import { createServer } from 'node:http'\nimport { healthResponse } from '../../src/shared/contracts.js'\n\nconst port = Number(process.env.PORT ?? 8787)\nconst server = createServer((request, response) => {\n  if (request.method === 'GET' && request.url === '/health') {\n    response.writeHead(200, { 'Content-Type': 'application/json; charset=utf-8' })\n    response.end(JSON.stringify(healthResponse('onevibe-generated-app')))\n    return\n  }\n  response.writeHead(404, { 'Content-Type': 'application/json; charset=utf-8' })\n  response.end(JSON.stringify({ error: 'Not found' }))\n})\nserver.listen(port, () => console.log(`Generated app server listening on ${port}`))\n",
    'app/server/README.md': '# Generated app server\n\nRun `npm run server:dev` for the local typed HTTP foundation. It intentionally has no credentials, connectors, or external side effects.\n',
  })
  for (const [file, content] of Object.entries(files)) await store.writeWorkspaceFile(task.id, file, content)
  const preview = storefront
    ? shell(task.title, `<div class="eyebrow">Generated storefront preview</div><h1>${escapeHtml(task.title)}</h1><p>${escapeHtml(task.prompt)}</p><div class="grid">${['Signal desk · $320', 'Atlas lamp · $185', 'Field notebook · $28'].map((product) => `<article class="card"><strong>${product}</strong><p>Fictional product data for prototype review.</p><button data-cart>Add to cart</button></article>`).join('')}</div><p id="cart" class="eyebrow">Cart · 0 items</p>`, `let count=0;document.querySelectorAll('[data-cart]').forEach(button=>button.addEventListener('click',()=>{count+=1;document.querySelector('#cart').textContent='Cart · '+count+' items'}))`)
    : dashboard
      ? shell(task.title, `<div class="eyebrow">Generated operations preview</div><h1>${escapeHtml(task.title)}</h1><p>${escapeHtml(task.prompt)}</p><p><button data-filter="all">All</button> <button data-filter="review">Needs review</button></p><div id="queue" class="grid"><article class="card" data-status="review"><strong>OPS-124 · Review workspace request</strong><p>Needs review · Nadia</p></article><article class="card" data-status="active"><strong>OPS-123 · Confirm delivery evidence</strong><p>In progress · Luca</p></article><article class="card" data-status="complete"><strong>OPS-119 · Archive completed brief</strong><p>Complete · Mina</p></article></div>`, `document.querySelectorAll('[data-filter]').forEach(button=>button.addEventListener('click',()=>{const filter=button.dataset.filter;document.querySelectorAll('[data-status]').forEach(item=>item.hidden=filter==='review'&&item.dataset.status!=='review')}))`)
      : website
        ? shell(task.title, `<div class="eyebrow">Generated website preview</div><h1>${escapeHtml(task.title)}</h1><p>${escapeHtml(task.prompt)}</p><div class="grid"><article class="card"><strong>Clear operating boundary</strong><p>Workspaces, policy, and evidence are visible by design.</p></article><article class="card"><strong>Reviewable delivery</strong><p>Portable source is available beside this preview.</p></article><article class="card"><strong>Human decision point</strong><p>Consequential actions remain outside the browser.</p></article></div>`)
        : undefined
  if (preview) await store.writeWorkspaceFile(task.id, 'index.html', preview)
  return Object.keys(files)
}

export const writeModeArtifacts = async (task: Task, store: TaskStore) => {
  if (task.mode === 'slides') return writeSlides(task, store)
  if (task.mode === 'document') {
    const document = `# ${task.title}\n\n## Purpose\n\n${task.prompt}\n\n## Executive summary\n\nThis portable document was drafted in a governed ONEVibe workspace. It separates the requested outcome from the operating boundary: actions that can change external state remain subject to policy and, where required, a separate wallet approval.\n\n## Recommended next steps\n\n1. Review the substantive content with the accountable owner.\n2. Validate source material and assumptions before external distribution.\n3. Request an external approval for any publication or connector write.\n\n## Provenance\n\nThis file is exported with the task evidence manifest.\n`
    await store.writeWorkspaceFile(task.id, 'document.md', document)
    await store.writeWorkspaceFile(task.id, 'document.json', `${JSON.stringify({ title: task.title, format: 'markdown', generatedBy: 'onevibe', requiresReview: true }, null, 2)}\n`)
    await store.writeWorkspaceFile(task.id, 'index.html', shell(task.title, `<div class="eyebrow">Governed document</div><h1>${escapeHtml(task.title)}</h1><div class="card"><strong>Executive summary</strong><p>${escapeHtml(task.prompt)}</p></div><div class="grid"><article class="card"><strong>Portable</strong><p>Markdown source is ready for review, editing, and export.</p></article><article class="card"><strong>Accountable</strong><p>External distribution remains subject to the configured approval policy.</p></article></div>`))
    return ['index.html', 'document.md', 'document.json']
  }
  if (task.mode === 'research') {
    await store.writeWorkspaceFile(task.id, 'report.md', `# ${task.title}\n\n## Research question\n\n${task.prompt}\n\n## Findings\n\nThis local demo establishes the evidence-oriented artifact contract. Native Claude mode performs the substantive research.\n\n## Limitations\n\nNo external sources were accessed by the deterministic demo runtime.\n`)
    await store.writeWorkspaceFile(task.id, 'sources.json', '[]\n')
    await store.writeWorkspaceFile(task.id, 'index.html', shell(task.title, `<div class="eyebrow">Evidence-backed research</div><h1>${escapeHtml(task.title)}</h1><div class="grid"><article class="card"><strong>Question</strong><p>${escapeHtml(task.prompt)}</p></article><article class="card"><strong>Boundary</strong><p>No external sources were accessed in demo mode.</p></article></div>`))
    return ['index.html', 'report.md', 'sources.json']
  }
  if (task.mode === 'data') {
    const rows = [['Stage', 'Workspaces'], ['Requested', '120'], ['Policy checked', '112'], ['Sandbox ready', '97'], ['Delivered', '84']]
    const csv = rows.map((row) => row.join(',')).join('\n') + '\n'
    const data = rows.slice(1).map(([label, value]) => ({ label, value: Number(value) }))
    const bars = data.map((item) => `<div class="bar"><span>${escapeHtml(item.label)}</span><i style="width:${item.value / 1.2}%"></i><b>${item.value}</b></div>`).join('')
    await store.writeWorkspaceFile(task.id, 'data.csv', csv)
    await store.writeWorkspaceFile(task.id, 'analysis.json', `${JSON.stringify({ metric: 'workspace progression', data, limitation: 'Deterministic sample data; connect an approved source for real analysis.' }, null, 2)}\n`)
    await store.writeWorkspaceFile(task.id, 'index.html', shell(task.title, `<style>.chart{margin-top:34px;padding:25px;border:1px solid #29332d;background:#101411}.chart h2{margin:0 0 22px;font-size:18px}.bar{display:grid;grid-template-columns:110px minmax(30px,1fr) 35px;gap:12px;align-items:center;margin:13px 0;color:#aeb8b1;font-size:13px}.bar i{height:11px;background:linear-gradient(90deg,#36dc7d,#91edb6);border-radius:99px}.bar b{color:#eaf2ec}.note{font-size:13px}</style><div class="eyebrow">Evidence-aware data story</div><h1>${escapeHtml(task.title)}</h1><p>${escapeHtml(task.prompt)}</p><section class="chart"><h2>Workspace progression</h2>${bars}</section><p class="note">Sample data only. The data.csv and analysis.json files make assumptions inspectable before any decision is made.</p>`))
    return ['index.html', 'data.csv', 'analysis.json']
  }
  if (task.mode === 'design') {
    const monogram = (task.title.match(/[A-Za-z0-9]/)?.[0] ?? 'O').toUpperCase()
    const directions = [
      { id: 'secure-signal', name: 'Secure Signal', rationale: 'Evidence-forward enterprise interface with clear operational status and decisive hierarchy.', confidence: 0.72, selected: true },
      { id: 'quiet-infrastructure', name: 'Quiet Infrastructure', rationale: 'Calm, sparse operational canvas that makes policy and infrastructure feel understandable.', confidence: 0.18, selected: false },
      { id: 'human-checkpoint', name: 'Human Checkpoint', rationale: 'Approval and accountable intent become the visual center of consequential workflows.', confidence: 0.10, selected: false },
    ]
    await store.writeWorkspaceFile(task.id, 'ideas.md', `# Design directions\n\n${directions.map((direction, index) => `${index + 1}. ${direction.name} — ${direction.rationale}`).join('\n')}\n\nSelected: Secure Signal.\n\nSelection confidence is a deterministic starter heuristic, not a model probability or user-research finding.\n`)
    await store.writeWorkspaceFile(task.id, 'design-directions.json', `${JSON.stringify({ title: task.title, selectionMethod: 'deterministic starter heuristic', philosophy: { name: 'Secure Signal', description: 'Evidence-forward enterprise interfaces: calm infrastructure, decisive status, and human approval at consequential boundaries.' }, directions }, null, 2)}\n`)
    await store.writeWorkspaceFile(task.id, 'design-tokens.json', `${JSON.stringify({ color: { background: '#090b0a', verified: '#38dc7d', pending: '#f1b84b' }, radius: { panel: 14 }, motion: { standard: 180 } }, null, 2)}\n`)
    await store.writeWorkspaceFile(task.id, 'brand-mark.svg', `<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 512 512" role="img" aria-label="Generated brand mark"><rect width="512" height="512" rx="112" fill="#090b0a"/><rect x="72" y="72" width="368" height="368" rx="80" fill="#111a14" stroke="#38dc7d" stroke-width="12"/><path d="M156 160v192l100-96 100 96V160l-100 96z" fill="#38dc7d"/><circle cx="256" cy="256" r="48" fill="#090b0a"/><text x="256" y="278" text-anchor="middle" fill="#e9f3ec" font-family="Arial, sans-serif" font-size="56" font-weight="700">${monogram}</text></svg>\n`)
    return ['ideas.md', 'design-directions.json', 'design-tokens.json', 'brand-mark.svg']
  }
  if (task.mode === 'website' || task.mode === 'app' || task.mode === 'game') return writeScaffold(task, store)
  return []
}
